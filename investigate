#!/usr/bin/env bash
# Quick entrypoint for the Company & Interviewer Intelligence Investigator

SCRIPT_DIR="$(cd "$(dirname "${BASH_SOURCE[0]}")" && pwd)"
python3 "${SCRIPT_DIR}/tools/investigate.py" "$@"

